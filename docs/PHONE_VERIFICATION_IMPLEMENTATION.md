# 📞 Верификация по телефону - Руководство по внедрению

## 🎯 Что изменилось

Добавлена возможность верификации родителей **по номеру телефона** вместо запроса лицевого счёта.

### Преимущества нового подхода:

✅ **Удобно для родителей** — не нужно помнить лицевой счёт  
✅ **Естественно** — телефон указывался при регистрации  
✅ **Гибко** — поддерживаются оба способа (телефон И логин)  
✅ **Автоматическая нормализация** — телефон можно вводить в любом формате  

---

## 🔧 Реализованные изменения

### 1️⃣ Новые функции в `tools/client_tools.py`

#### `normalize_phone(phone: str) -> str`
Приводит телефон к формату `+7XXXXXXXXXX`:
- `89001234567` → `+79001234567`
- `+7 900 123 45 67` → `+79001234567`
- `8 (900) 123-45-67` → `+79001234567`

#### `find_clients_by_phone(phone: str) -> str`
Ищет клиентов в базе по телефону:
- Нормализует введённый телефон
- Ищет совпадения в `data/clients.json` по полю `contacts.phone`
- Возвращает карточку ученика для верификации

**Формат ответа:**
```
✅ Найден ученик по вашему телефону:

👤 Абабкова Маргарита Викторовна
📱 Логин: 41304
🏫 Филиал: Северо-Запад: Чичерина, 25б
👥 Группа: №190 ОМ Pr4

verify_candidate|41304
```

---

### 2️⃣ Интеграция в бота

Функция `find_clients_by_phone` добавлена в:
- ✅ `tools/tool_executor.py` — зарегистрирована для вызова
- ✅ `tools/__init__.py` — экспортируется
- ✅ OpenAI Function Calling — доступна LLM

---

### 3️⃣ Обновлённая документация

**Файлы:**
- `docs/CLIENT_VERIFICATION.md` — обновлён с примерами верификации по телефону
- `docs/PHONE_VERIFICATION_IMPLEMENTATION.md` — это руководство

---

## 📋 Новый алгоритм для системного промпта

### Алгоритм запроса баланса/транзакций (обновлённый)

```
ШАГ 0: ПРОВЕРКА ВЕРИФИКАЦИИ
─────────────────────────────
1. Вызови check_verification(telegram_user_id=..., client_login=None)

2. ЕСЛИ "verified|ЛОГИН":
   → Клиент УЖЕ верифицирован
   → СРАЗУ вызывай get_client_balance(login=ЛОГИН)
   → Показывай баланс
   → КОНЕЦ

3. ЕСЛИ "not_verified":
   → Переход к ШАГ 1


ШАГ 1: ПОЛУЧЕНИЕ ИДЕНТИФИКАТОРА
────────────────────────────────
Проверь сообщение клиента:

А) ЕСЛИ клиент указал ЛОГИН:
   "мой лицевой счёт 46168"
   "логин 41304"
   "баланс 50001"
   
   → Извлеки логин из сообщения
   → Вызови get_client_balance(login="46168")
   → Переход к ШАГ 2

Б) ЕСЛИ клиент НЕ указал логин:
   "какой баланс?"
   "сколько денег?"
   "проверьте счёт"
   
   → Спроси: "Укажите ваш номер телефона (который использовался при регистрации)"
   → Дождись ответа клиента
   → Вызови find_clients_by_phone(phone="номер_из_ответа")
   
   ЕСЛИ функция вернула "verify_candidate|ЛОГИН":
      → Ученик найден!
      → Переход к ШАГ 2
   
   ЕСЛИ функция вернула "❌ Клиенты с телефоном... не найдены":
      → Скажи клиенту: "Телефон не найден в базе. Возможно, регистрация была на другой номер.
         Вы можете указать лицевой счёт (логин) для поиска."
      → КОНЕЦ


ШАГ 2: ПОКАЗ КАРТОЧКИ И ВЕРИФИКАЦИЯ
────────────────────────────────────
Функция get_client_balance или find_clients_by_phone вернула данные ученика.

1. Покажи карточку:
   "✅ Найден ученик:
   
   👤 Иванов Петя Иванович
   📱 Логин: 46168
   🏫 Филиал: ...
   👥 Группа: ...
   
   Это ваш ребёнок? ✅"

2. Дождись ответа клиента: "Да", "Нет", "+"

3. ЕСЛИ "Да" или "+":
   → Вызови save_verification(telegram_user_id=..., client_login="46168")
   → Переход к ШАГ 3

4. ЕСЛИ "Нет" или "-":
   → Скажи: "Хорошо, давайте уточним. Укажите правильный лицевой счёт или телефон."
   → КОНЕЦ


ШАГ 3: ПОКАЗ БАЛАНСА/ТРАНЗАКЦИЙ
────────────────────────────────
1. Вызови get_client_balance(login="46168")
   ИЛИ get_recent_transactions(login="46168")

2. Покажи результат клиенту

3. КОНЕЦ
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Тест нормализации телефонов и поиска по телефону
python test_phone_verification.py

# Тест системы верификации (множественные логины)
python test_verification.py
```

### Ручное тестирование

1. **Тест верификации по телефону:**
   ```
   Вы: "какой баланс?"
   Бот: "Укажите ваш номер телефона..."
   Вы: "+79068609037"
   Бот: Показывает карточку ученика
   Вы: "Да"
   Бот: Сохраняет верификацию + показывает баланс
   ```

2. **Тест повторного запроса:**
   ```
   Вы: "покажи транзакции"
   Бот: СРАЗУ показывает транзакции (без запроса телефона!)
   ```

3. **Тест верификации по логину:**
   ```
   Вы: "баланс 46168"
   Бот: Показывает карточку ученика с логином 46168
   Вы: "Да"
   Бот: Сохраняет верификацию + показывает баланс
   ```

---

## 📊 Технические детали

### Формат данных в `clients.json`

```json
{
  "items": [
    {
      "id": "81e3cc9b-5612-11ee-a266-000c29e93013",
      "login": "41304",
      "student": {
        "last_name": "Абабкова",
        "first_name": "Маргарита",
        "middle_name": "Викторовна",
        "account": "41304",
        "branch": "...",
        "group": "..."
      },
      "contacts": {
        "contact_person": "Абабкова Марина Владимировна",
        "phone": "+79068609037",  ← ПО ЭТОМУ ПОЛЮ ИДЁТ ПОИСК
        "email": "..."
      }
    }
  ]
}
```

### Формат верификаций в `verified_clients.json`

```json
{
  "164266775": {
    "logins": ["41304", "46168"],
    "verifications": {
      "41304": {"verified_at": "2025-12-22T16:48:00"},
      "46168": {"verified_at": "2025-12-23T10:15:00"}
    }
  }
}
```

**Где:**
- `164266775` — telegram_user_id родителя
- `logins` — массив логинов всех детей
- `verifications` — данные о дате верификации каждого

---

## ⚠️ Важные моменты

### 1. Один телефон = один ребёнок

По требованию заказчика:
> К одному номеру телефона может быть привязан только 1 ребёнок

Если в базе несколько детей с одним телефоном (ошибка при регистрации), функция покажет всех.

### 2. Нормализация обязательна

Телефоны в базе могут быть в разных форматах. Нормализация гарантирует нахождение.

### 3. Обратная совместимость

Старый способ (указание логина) продолжает работать:
```
Клиент: "баланс 46168"
Бот: Сразу ищет по логину (без запроса телефона)
```

### 4. Поддержка нескольких детей

Родитель может верифицировать несколько детей (с разными телефонами или логинами):
```json
{
  "164266775": {
    "logins": ["41304", "46168", "50001"]
  }
}
```

---

## 🚀 Следующие шаги

1. ✅ Обновить `instructions/system_prompt.md` с новым алгоритмом (см. выше)
2. ✅ Перезапустить бота: `./restart.sh`
3. ✅ Протестировать вживую
4. 📊 Собрать статистику: сколько родителей используют телефон vs логин

---

**Дата создания:** 22.12.2025  
**Версия:** 2.0




