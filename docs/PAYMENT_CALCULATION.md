# Расчет платежа на следующий месяц

## Описание

Функция `calculate_next_month_payment` автоматически рассчитывает сумму, которую клиенту нужно внести для оплаты занятий в следующем календарном месяце.

## Как работает

### 1. Сбор данных
- Получает текущий баланс клиента из контракта (`contracts.json`)
- Находит группу клиента в `clients.json` → извлекает номер группы
- Находит расписание группы в `groups.json` (дни недели: пн, вт, ср, чт, пт, сб, вс)
- Определяет филиал → выбирает тариф из `prices.json` (standard/reduced)

### 2. Подсчет занятий
Функция считает **реальное** количество занятий в следующем календарном месяце:
- Если расписание: **вт/чт** → считает все вторники и четверги в январе
- Пример: январь 2026 = 9 занятий (4 вторника + 5 четвергов)
- Учитывает календарь, но **НЕ** учитывает праздники (это запас для буфера)

⚠️ **Новогодние каникулы 2026:**
- **1-9 января 2026** — занятий НЕТ
- **10-11 января 2026** — занятия по согласованию с педагогом
- **С 12 января 2026** — занятия по расписанию

### 3. Формула расчета

```
Необходимая сумма = (Количество_занятий × Стоимость_занятия) - Текущий_баланс + 1000₽
```

**Примеры:**

**Пример 1: Клиент с долгом**
```
👩 Маргарита, №190 ОМ Pr4 вт/чт Чичерина 25-26

💰 Текущий баланс: -1600 ₽

📆 В январе запланировано 9 занятий по 800 ₽
📊 Стоимость месяца: 9 × 800 ₽ = 7200 ₽

⚠️ С учётом текущего минуса 1600 ₽ и небольшого запаса, рекомендуем оплатить 9800 ₽.

📝 Как получилась сумма:
• Занятия: 7200 ₽
• Погасить долг: +1600 ₽
• Запас на случай задержки: +1000 ₽
━━━━━━━━━━━━━━━━━━━
Итого к оплате: 9800 ₽

✅ После оплаты на счёте останется около 1000 ₽ — хватит с запасом 😊
```

**Пример 2: Клиент с положительным балансом**
```
👩 Анна, №45 PE5 вт/чт Парковый 25-26

💰 Текущий баланс: +2000 ₽

📆 В феврале запланировано 8 занятий по 800 ₽
📊 Стоимость месяца: 8 × 800 ₽ = 6400 ₽

✅ С учётом вашего баланса 2000 ₽ и небольшого запаса, рекомендуем оплатить 5400 ₽.

📝 Как получилась сумма:
• Занятия: 6400 ₽
• Вычесть баланс: -2000 ₽
• Запас на случай задержки: +1000 ₽
━━━━━━━━━━━━━━━━━━━
Итого к оплате: 5400 ₽

✅ После оплаты на счёте останется около 1000 ₽ — хватит с запасом 😊
```

**Пример 3: Клиент с нулевым балансом**
```
👩 Света, №120 Sol3 сб Центр 25-26

💰 Текущий баланс: 0 ₽

📆 В марте запланировано 4 занятия по 1230 ₽
📊 Стоимость месяца: 4 × 1230 ₽ = 4920 ₽

✅ С учётом небольшого запаса, рекомендуем оплатить 5920 ₽.

📝 Как получилась сумма:
• Занятия: 4920 ₽
• Запас на случай задержки: +1000 ₽
━━━━━━━━━━━━━━━━━━━
Итого к оплате: 5920 ₽

✅ После оплаты на счёте останется около 1000 ₽ — хватит с запасом 😊
```

### 4. Буфер 1000₽

Зачем нужен:
- ✅ Защита от задержки следующего платежа (не будет минуса на балансе)
- ✅ Учет возможных праздников/каникул (расчет может быть +1 занятие)
- ✅ Спокойствие клиента: платеж не заблокируется из-за 100₽

## Использование в боте

### Триггеры для GPT
Функция вызывается автоматически, когда клиент спрашивает:
- "Сколько нужно заплатить?"
- "Сколько платить в следующем месяце?"
- "Какую сумму внести?"
- "Сколько будет стоить следующий месяц?"
- "Рассчитайте оплату"

### Пример диалога

```
Клиент: Какой у меня баланс?
Бот: ✅ Баланс: -1600 ₽

Клиент: Сколько нужно заплатить на следующий месяц?
Бот: 👩 Маргарита, №190 ОМ Pr4 вт/чт Чичерина 25-26

💰 Текущий баланс: -1600 ₽

📆 В январе запланировано 9 занятий по 800 ₽
📊 Стоимость месяца: 9 × 800 ₽ = 7200 ₽

⚠️ С учётом текущего минуса 1600 ₽ и небольшого запаса, рекомендуем оплатить 9800 ₽.

📝 Как получилась сумма:
• Занятия: 7200 ₽
• Погасить долг: +1600 ₽
• Запас на случай задержки: +1000 ₽
━━━━━━━━━━━━━━━━━━━
Итого к оплате: 9800 ₽

✅ После оплаты на счёте останется около 1000 ₽ — хватит с запасом 😊
```

## Верификация

Функция требует верификации клиента:
- ✅ Автоматически проверяет `telegram_user_id`
- ✅ Если не верифицирован → запрашивает телефон/логин
- ✅ Если несколько детей → запрашивает выбор ребенка

## Edge Cases

| Ситуация | Поведение |
|----------|-----------|
| Группа не найдена в `groups.json` | Использует стандарт 8 занятий (2 раза в неделю) |
| Расписание не найдено | Определяет по названию группы (сб/вс → 4 занятия) |
| Цена не найдена в `prices.json` | Использует среднюю: 800₽ (будни) или 1200₽ (выходные) |
| Положительный баланс | Вычитается из суммы платежа |
| Отрицательный баланс (долг) | Добавляется к сумме платежа |

## Технические детали

### Определение следующего месяца
```python
now = datetime.now()
if now.month == 12:
    next_month = 1
    next_year = now.year + 1
else:
    next_month = now.month + 1
    next_year = now.year
```

### Подсчет занятий
```python
days_ru_to_en = {
    'пн': 0, 'вт': 1, 'ср': 2, 'чт': 3, 'пт': 4, 'сб': 5, 'вс': 6
}

lessons_count = 0
num_days = monthrange(next_year, next_month)[1]

for day in range(1, num_days + 1):
    date = datetime(next_year, next_month, day)
    weekday = date.weekday()
    
    for schedule_day in days:
        if days_ru_to_en.get(schedule_day) == weekday:
            lessons_count += 1
            break
```

### Определение тарифа
```python
reduced_districts = ['чмз', 'хмельницкого', 'чурилово', 'зальцмана', 'копейск']
is_reduced = any(district in branch_lower for district in reduced_districts)
tier = 'reduced' if is_reduced else 'standard'
```

## Обновление данных

Функция работает с:
- `data/clients.json` — обновляется ежечасно
- `data/contracts.json` — обновляется ежечасно
- `data/groups.json` — обновляется ежедневно
- `data/prices.json` — статичный файл (обновляется вручную)

## Ограничения

⚠️ **НЕ учитывается:**
- Праздничные дни (Новый год, 23 февраля, 8 марта и т.д.)
- Каникулы (зимние, весенние, летние)
- Индивидуальные переносы занятий

Эти факторы компенсируются **буфером 1000₽**.

## История версий

### v1.0 (2025-12-23)
- ✅ Точный календарный подсчет количества занятий
- ✅ Автоматическое определение тарифа по филиалу
- ✅ Учет текущего баланса (положительный/отрицательный)
- ✅ Буфер 1000₽ на случай задержки оплаты
- ✅ Интеграция с системой верификации

