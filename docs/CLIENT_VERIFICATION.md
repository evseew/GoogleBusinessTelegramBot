# 🔐 Система верификации клиентов

## Описание

Система автоматически сохраняет связки `telegram_user_id` ↔ `[client_logins]` после первого успешного подтверждения клиентом, что это данные его ребёнка.

**Два способа верификации:**
1. **По телефону** (рекомендуется) — родитель указывает номер телефона, который использовался при регистрации
2. **По логину** (альтернатива) — родитель указывает лицевой счёт напрямую

**Цель:** Избежать повторной верификации при каждом запросе баланса или транзакций.

---

## 🎯 Как это работает

### 🆕 Верификация по телефону (новый способ)

#### Первый запрос клиента

```
Клиент: "какой баланс?"

Бот:
1. ✅ Вызывает check_verification(telegram_user_id=123456, client_login=None)
   → Результат: "not_verified" (клиент НЕ верифицирован)

2. 📞 Просит телефон: "Укажите ваш номер телефона (который использовался при регистрации)"

Клиент: "+79001234567" (или 89001234567, или 9001234567)

3. 🔍 Вызывает find_clients_by_phone(phone="+79001234567")
   → Нормализует телефон: +79001234567
   → Ищет в базе clients.json по полю contacts.phone
   → Находит ученика

4. 📋 Показывает карточку для верификации:
   "✅ Найден ученик по вашему телефону:
   
   👤 Абабкова Маргарита Викторовна
   📱 Логин: 41304
   🏫 Филиал: Северо-Запад: Чичерина, 25б
   👥 Группа: №190 ОМ Pr4
   
   Это ваш ребёнок? ✅"

5. ⏳ Ждёт подтверждения

Клиент: "да"

6. 💾 Вызывает save_verification(telegram_user_id=123456, client_login="41304")
   → Сохраняет связку в data/verified_clients.json

7. 💰 Показывает баланс:
   "Отлично! 💫
   
   💰 Баланс: -4360 руб.
   🎁 Бонусы: 4940"
```

#### Повторный запрос

```
Клиент: "какие последние транзакции?"

Бот:
1. ✅ Вызывает check_verification(telegram_user_id=123456, client_login="41304")
   → Результат: "verified|41304"

2. 📜 СРАЗУ показывает транзакции БЕЗ запроса телефона!
```

---

### 📱 Верификация по логину (альтернативный способ)

#### Первый запрос клиента

```
Клиент: "мой лицевой счёт 46168 какой баланс?"

Бот:
1. ✅ Вызывает check_verification(telegram_user_id=123456, client_login="46168")
   → Результат: "not_verified|46168" (клиент НЕ подтверждал ранее)

2. 🔍 Вызывает get_client_balance(login="46168") для получения данных

3. 📋 Показывает карточку для верификации:
   "Хорошо 😊 Сначала уточню:
   
   👤 Абдулаева Аиша
   🏫 Филиал: ЧМЗ: Б.Хмельницкого, 19
   👥 Группа: №66 Pr2
   
   Это данные вашего ребёнка? ✅"

4. ⏳ Ждёт подтверждения клиента

Клиент: "да"

5. 💾 Вызывает save_verification(telegram_user_id=123456, client_login="46168")
   → Сохраняет связку в data/verified_clients.json

6. 💰 Показывает баланс:
   "Отлично! 💫
   
   💰 Баланс: -4360 руб.
   🎁 Бонусы: 4940"
```

---

### Повторный запрос того же клиента

```
Клиент: "какие последние транзакции?"

Бот:
1. ✅ Вызывает check_verification(telegram_user_id=123456, client_login="46168")
   → Результат: "verified|46168" (клиент УЖЕ подтверждал!)

2. 📜 СРАЗУ вызывает get_recent_transactions(login="46168")

3. ✅ Показывает транзакции БЕЗ верификации:
   "📜 Последние операции по счёту 46168:
   
   1. 18.12.2025 | ➖ 800 руб.
      Списание за занятия
   ..."
```

→ **НЕТ повторной верификации!** Клиент уже подтверждал этот логин с этого телефона.

---

## 📞 Нормализация телефонных номеров

Система автоматически приводит все форматы телефонов к единому виду `+7XXXXXXXXXX`:

| Формат ввода | Результат |
|--------------|-----------|
| `+79001234567` | `+79001234567` ✅ |
| `89001234567` | `+79001234567` ✅ |
| `9001234567` | `+79001234567` ✅ |
| `+7 900 123 45 67` | `+79001234567` ✅ |
| `8 (900) 123-45-67` | `+79001234567` ✅ |
| `+7-900-123-45-67` | `+79001234567` ✅ |

**Преимущества:**
- ✅ Родитель может ввести телефон в любом удобном формате
- ✅ Система всегда найдёт совпадение
- ✅ Защита от опечаток в форматировании

---

## 📁 Структура хранения

**Файл:** `data/verified_clients.json`

**Формат:** (поддержка нескольких детей у одного родителя)
```json
{
  "123456789": {
    "logins": ["46168", "50001"],
    "verifications": {
      "46168": {"verified_at": "2025-12-22T16:30:45.123456"},
      "50001": {"verified_at": "2025-12-23T10:15:20.654321"}
    }
  },
  "987654321": {
    "logins": ["41304"],
    "verifications": {
      "41304": {"verified_at": "2025-12-21T14:20:10.654321"}
    }
  }
}
```

**Где:**
- `123456789` — telegram_user_id (ID пользователя в Telegram)
- `logins` — массив лицевых счетов (логинов) всех детей родителя
- `verifications` — данные верификации для каждого логина
- `verified_at` — дата и время верификации (ISO формат)

**Преимущества:**
- ✅ Родитель с несколькими детьми верифицирует каждого **один раз**
- ✅ Переключение между детьми без повторной верификации
- ✅ Автоматическая миграция из старого формата

---

## ⏰ Срок действия верификации

**По умолчанию:** 90 дней (3 месяца)

**Настройка:** `tools/verification_tools.py` → `VERIFICATION_EXPIRY_DAYS`

```python
VERIFICATION_EXPIRY_DAYS = 90  # Измените на нужный срок или None для бессрочной
```

**Автоматическое удаление:** Истёкшие верификации удаляются автоматически при проверке.

---

## 🔧 Команды бота

### `/reset_verification` — Сбросить верификацию

**Доступна:** Всем клиентам

**Использование:** Клиент может сбросить свою верификацию если:
- Сменил телефон
- Ошибочно подтвердил чужой логин
- Хочет заново пройти верификацию

**Форматы команды:**

1. **Сбросить ВСЕ верификации:**
```
Клиент: /reset_verification
Бот: "✅ Все верификации сброшены (было детей: 2)"
```

2. **Сбросить конкретный логин:**
```
Клиент: /reset_verification 46168
Бот: "✅ Верификация сброшена для логина 46168 (осталось детей: 1)"
```

После сброса при следующем запросе баланса для этого логина клиенту нужно будет заново подтвердить данные.

---

### `/list_verifications` — Показать все верификации (только админ)

**Доступна:** Только для админа (ADMIN_USER_ID)

**Использование:** Просмотр всех активных верификаций для отладки.

**Пример:**
```
Админ: /list_verifications
Бот: 
"📋 Всего пользователей: 2
📋 Всего верифицированных логинов: 3

👤 Telegram ID: 123456789
   👶 Детей: 2
   • Логин: 46168
     Дата: 22.12.2025 16:30
   • Логин: 50001
     Дата: 23.12.2025 10:15

👤 Telegram ID: 987654321
   👶 Детей: 1
   • Логин: 41304
     Дата: 21.12.2025 14:20"
```

---

## 👨‍👩‍👧‍👦 Сценарий: Несколько детей у одного родителя

### Первый ребёнок

```
Родитель: "баланс 46168"

Бот:
1. check_verification → not_verified
2. Показывает карточку ребёнка №1
3. Родитель: "Да"
4. save_verification → Сохраняет "46168"
5. Показывает баланс

📌 Сохранено в базе:
{
  "123456789": {
    "logins": ["46168"],
    "verifications": {
      "46168": {"verified_at": "..."}
    }
  }
}
```

### Второй ребёнок (новая верификация)

```
Родитель: "баланс 50001"

Бот:
1. check_verification → not_verified (логин 50001 ещё не верифицирован)
2. Показывает карточку ребёнка №2
3. Родитель: "Да"
4. save_verification → Добавляет "50001" в список
5. Показывает баланс

✅ Сообщение: "Верификация сохранена для логина 50001 (всего детей: 2)"

📌 Сохранено в базе:
{
  "123456789": {
    "logins": ["46168", "50001"],
    "verifications": {
      "46168": {"verified_at": "..."},
      "50001": {"verified_at": "..."}
    }
  }
}
```

### Переключение между детьми (без верификации!)

```
Родитель: "транзакции 46168"
Бот: check_verification → verified ✅ Сразу показывает данные!

Родитель: "баланс 50001"
Бот: check_verification → verified ✅ Сразу показывает данные!
```

→ **Верификация больше НЕ требуется!** 🎉

---

## 🔐 Безопасность

### Что защищаем:

1. **Конфиденциальность финансовых данных:**
   - Баланс, бонусы, транзакции показываются только после подтверждения

2. **Защита от ошибок:**
   - Клиент не может случайно увидеть чужие данные

3. **Удобство для клиента:**
   - Подтверждение требуется только **ОДИН РАЗ** с конкретного телефона

### Что НЕ защищает:

⚠️ **Важно:** Верификация привязана к `telegram_user_id`, а не к номеру телефона!

**Сценарий риска:**
- Если клиент сменил номер телефона в Telegram, но у него тот же аккаунт → верификация сохранится
- Если клиент создал **новый** аккаунт Telegram → потребуется новая верификация

**Рекомендация:** Срок действия 90 дней минимизирует риски.

---

## 🛠️ Интеграция в системный промпт

### Функции для OpenAI

#### `check_verification`

**Когда использовать:** ВСЕГДА перед запросом баланса или транзакций!

**Параметры:**
- `telegram_user_id` (int) — ID пользователя в Telegram
- `client_login` (string) — Логин клиента

**Возвращает:**
- `"verified|46168"` — клиент подтверждал
- `"not_verified|46168"` — клиент НЕ подтверждал

**Пример в промпте:**
```
ШАГ 0: СНАЧАЛА проверь верификацию
check_verification(telegram_user_id=..., client_login="46168")

Если "verified|..." → ПРОПУСКАЙ шаги верификации, сразу показывай данные
Если "not_verified|..." → показывай карточку и проси подтверждение
```

---

#### `save_verification`

**Когда использовать:** СРАЗУ после подтверждения клиента "Да"

**Параметры:**
- `telegram_user_id` (int) — ID пользователя в Telegram
- `client_login` (string) — Логин, который клиент подтвердил

**Возвращает:**
- `"✅ Верификация сохранена для логина 46168"`

**Пример в промпте:**
```
Клиент: "Да"
Бот: [save_verification(telegram_user_id=..., client_login="46168")]
Бот: "Отлично! 💫"
```

---

## 📊 Мониторинг

### Логи

Все операции верификации логируются:

```
[INFO] ✅ Верификация сохранена: telegram_user=123456, login=46168
[INFO] ✅ Верификация найдена и актуальна: telegram_user=123456, login=46168
[INFO] Верификация НЕ найдена для telegram_user=789012
[INFO] Верификация истекла для telegram_user=111222, login=12345
[INFO] Верификация сброшена для telegram_user=123456, login=46168
```

### Метрики

Можно отслеживать:
- Количество верификаций: `/list_verifications` (админ)
- Процент запросов БЕЗ верификации (повторные клиенты)
- Количество сбросов верификаций

---

## 🚨 Устранение проблем

### Проблема: Бот каждый раз просит верификацию

**Причины:**
1. ❌ Бот НЕ вызывает `check_verification` перед запросом баланса
2. ❌ Бот НЕ вызывает `save_verification` после подтверждения клиента
3. ❌ Файл `data/verified_clients.json` повреждён или недоступен
4. ❌ `telegram_user_id` передаётся неправильно (None, 0, или не тот ID)

**Решение:**
- Проверьте логи: есть ли сообщения "Верификация сохранена"?
- Проверьте файл: `cat data/verified_clients.json`
- Убедитесь, что `telegram_user_id` правильно передаётся в функции

---

### Проблема: Верификация не сбрасывается

**Причины:**
1. ❌ Клиент не выполнил `/reset_verification`
2. ❌ Файл защищён от записи

**Решение:**
- Выполните `/reset_verification` от имени клиента
- Проверьте права доступа к файлу: `ls -l data/verified_clients.json`

---

### Проблема: Клиент сменил телефон, но видит старую верификацию

**Объяснение:** 
Верификация привязана к `telegram_user_id`, а не к номеру телефона.
Если клиент сменил номер в существующем аккаунте Telegram → ID не изменился.

**Решение для клиента:**
1. Выполнить `/reset_verification`
2. Заново пройти верификацию при следующем запросе баланса

---

## 📝 Обновление системного промпта

После внедрения системы верификации системный промпт обновлён:

### Изменения в алгоритме `get_client_balance`:

**БЫЛО (3 шага):**
1. Найди ученика → покажи карточку
2. Дождись верификации
3. Покажи баланс

**СТАЛО (4 шага):**
0. **ШАГ 0:** Проверь верификацию через `check_verification`
   - Если `verified` → сразу к шагу 3
   - Если `not_verified` → к шагу 1
1. Покажи карточку для верификации
2. Дождись "Да" → вызови `save_verification`
3. Покажи баланс

### Аналогично для `get_recent_transactions`

---

## ✅ Контрольный список при развёртывании

- [ ] Файл `tools/verification_tools.py` создан
- [ ] Функции добавлены в `tools/tool_executor.py`
- [ ] Файл `data/verified_clients.json` создан с правами записи
- [ ] Команды `/reset_verification` и `/list_verifications` добавлены в бота
- [ ] Системный промпт обновлён с инструкциями по `check_verification` и `save_verification`
- [ ] Бот перезапущен: `./restart.sh`
- [ ] Протестирована первая верификация клиента
- [ ] Протестирован повторный запрос БЕЗ верификации
- [ ] Протестирована команда `/reset_verification`

---

## 📚 Связанные файлы

- `tools/verification_tools.py` — логика верификации
- `tools/tool_executor.py` — регистрация функций для OpenAI
- `tools/client_tools.py` — функции работы с балансом и транзакциями
- `instructions/system_prompt.md` — инструкции для бота
- `bot.py` — команды `/reset_verification` и `/list_verifications`
- `data/verified_clients.json` — хранилище верификаций

---

**Дата создания:** 22.12.2025  
**Версия:** 1.0
