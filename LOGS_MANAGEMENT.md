# 📋 Управление логами Google Business Bot

## 📊 Текущие лог-файлы

- **`logs/bot.log`** — основной лог бота (все события, ошибки, API вызовы)
- **`logs/db_update.log`** — логи обновления базы знаний
- **`logs/supervisor.out`** — вывод супервизора (если используется)
- **`logs/context_logs/`** — логи контекста запросов пользователей (по каждому запросу)

---

## 🧹 Методы очистки

### 1️⃣ Ручная очистка (с архивированием)

**Самый простой способ:**
```bash
./control.sh clean
```

Или напрямую:
```bash
./clean_logs.sh
```

**Что происходит:**
- ✅ Архивирует текущие логи в `logs/archive/` (с gzip сжатием)
- ✅ Очищает основные лог-файлы
- ✅ Удаляет старые context_logs (> 7 дней)
- ✅ Удаляет старые архивы (> 30 дней)

---

### 2️⃣ Автоматическая ротация (logrotate)

**Настроена автоматическая ротация через cron:**
- ⏰ **Каждый день в 1:00** запускается `rotate_logs.sh`
- 📦 Ротация происходит **ежедневно**
- 💾 Хранятся **30 последних дней** (сжатые)

**Конфигурация:** `logrotate.conf`

**Ручной запуск ротации:**
```bash
./rotate_logs.sh
```

---

### 3️⃣ Быстрая очистка без архивирования

**⚠️ ВНИМАНИЕ: Удаляет логи без возможности восстановления!**

```bash
# Просто очистить bot.log
> logs/bot.log

# Или удалить и пересоздать
rm logs/bot.log && touch logs/bot.log
```

---

## ⏰ Автоматизация (cron)

**Текущее расписание** (см. `crontab_entry.txt`):

```
# Ежедневная ротация логов в 1:00
0 1 * * * ./rotate_logs.sh

# Обновление групп в 2:00
0 2 * * * ./scripts/update_groups.sh

# Обновление базы знаний в 3:00
0 3 * * * ./update_db.sh

# Перезапуск бота по воскресеньям в 4:00
0 4 * * 0 ./restart.sh
```

**Установить cron-задачи:**
```bash
crontab crontab_entry.txt
```

---

## 📈 Мониторинг размера логов

**Посмотреть размер всех логов:**
```bash
du -sh logs/
du -sh logs/archive/
```

**Список больших файлов:**
```bash
find logs/ -type f -size +1M -ls
```

**Статистика по лог-файлам:**
```bash
ls -lh logs/*.log
wc -l logs/*.log
```

---

## 🔍 Просмотр логов

**Последние 50 строк:**
```bash
./control.sh logs
# ИЛИ
tail -50 logs/bot.log
```

**В реальном времени:**
```bash
tail -f logs/bot.log
```

**Поиск ошибок:**
```bash
grep ERROR logs/bot.log | tail -20
grep CRITICAL logs/bot.log
```

**Логи за сегодня:**
```bash
grep "$(date +%Y-%m-%d)" logs/bot.log
```

---

## 🗂️ Структура архивов

После ротации/очистки файлы сохраняются в:
```
logs/archive/
├── bot_20251222_010000.log.gz      # Архив от 22.12.2025 01:00
├── db_update_20251222_010000.log.gz
└── supervisor_20251222_010000.log.gz
```

**Восстановить архив:**
```bash
gunzip -c logs/archive/bot_20251222_010000.log.gz > restored.log
```

---

## ⚙️ Настройка ротации

**Изменить параметры ротации** (`logrotate.conf`):

```conf
# Ротация по времени (текущая настройка)
daily             # Каждый день
rotate 30         # Хранить 30 дней

# Альтернативные варианты:
# weekly          # Каждую неделю
# monthly         # Каждый месяц
# size 10M        # По размеру (когда файл достигнет 10MB)

# Сжатие
compress          # Сжимать старые логи
delaycompress     # Не сжимать последний файл (на случай дозаписи)
```

---

## 🚨 Устранение проблем

### Лог растёт слишком быстро

**Проверить причину:**
```bash
# Найти самые частые сообщения
sort logs/bot.log | uniq -c | sort -rn | head -20
```

**Если много дублей:**
- Проверьте, нет ли нескольких запущенных экземпляров бота
- Уменьшите уровень логирования в `bot.py`

### Диск заполнен логами

**Очистить всё (экстренно):**
```bash
# Удалить все старые логи и архивы
rm -rf logs/archive/*
> logs/bot.log
> logs/db_update.log
find logs/context_logs/ -name "*.txt" -delete
```

### Logrotate не работает

**Проверить:**
```bash
# Запустить вручную с отладкой
/usr/sbin/logrotate -d logrotate.conf

# Проверить cron
crontab -l | grep rotate
```

---

## 📌 Рекомендации

✅ **Рекомендуемая стратегия:**
- Автоматическая ротация через cron (уже настроена)
- Ручная очистка раз в месяц: `./control.sh clean`
- Мониторинг размера диска: `df -h`

⚠️ **Важно:**
- Архивы хранятся **30 дней**, затем удаляются автоматически
- Context_logs хранятся **7 дней** (можно изменить в `clean_logs.sh`)
- При проблемах всегда можно восстановить из архивов

---

## 📞 Быстрые команды

```bash
# Посмотреть размер логов
du -sh logs/

# Очистить с архивированием
./control.sh clean

# Просмотр в реальном времени
./control.sh logs

# Найти ошибки
grep ERROR logs/bot.log | tail -20
```

---

**Дата последнего обновления:** 22.12.2025
