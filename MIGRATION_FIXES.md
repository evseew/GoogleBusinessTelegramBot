# ✅ Исправления миграции на OpenAI Responses API (Вариант B)

## 🛠️ Что было исправлено:

### 1. Команда `/reset` (строки 1050-1077)
**Было:** Очищался только `user_threads` (старый API)  
**Стало:** Очищаются **ОБА** хранилища:
- `user_threads` (legacy API)
- `user_conversations` (новый API)

**Результат:** Пользователь может сбросить диалог независимо от того, какой API активен.

---

### 2. Команда `/reset_all` (строки 1075-1120)
**Было:** 
- Очищался только `user_threads`
- Удалялся только файл `user_threads.json`

**Стало:**
- Очищаются **ОБА** хранилища в памяти
- Удаляются **ОБА** файла: `user_threads.json` И `user_conversations.json`
- В ответе показывается счетчик для обоих хранилищ

**Результат:** Полный сброс всех данных для обоих API.

---

### 3. Автомиграция при старте (строки 1538-1558)
**Добавлена логика:**
```
Если USE_OPENAI_RESPONSES=True:
  Если есть user_threads.json, но НЕТ user_conversations.json:
    → Создать пустой user_conversations.json
    → Залогировать предупреждение с количеством пользователей
```

**Результат:** 
- При первом запуске с новым API пользователи начнут с чистых conversations
- Старые threads остаются в файле для возможного отката
- Контролируемая миграция без потери данных

---

### 4. Обновлена версия OpenAI SDK (requirements.txt)
**Было:** `openai>=1.50.0`  
**Стало:** `openai>=1.56.0`

**Причина:** Responses/Conversations API доступен только с версии 1.56.0+

---

## 🚀 Как деплоить на сервер:

### Шаг 1: Загрузить файлы
```bash
scp bot.py user@server:/path/to/GoogleBusinessBot/
scp requirements.txt user@server:/path/to/GoogleBusinessBot/
```

### Шаг 2: На сервере обновить зависимости
```bash
cd /path/to/GoogleBusinessBot
source new_venv/bin/activate
pip install --upgrade -r requirements.txt
```

### Шаг 3: Проверить .env файл
```bash
# Убедитесь, что переменная установлена (сейчас у вас False)
grep USE_OPENAI_RESPONSES .env
# Должно быть: USE_OPENAI_RESPONSES="False"
```

### Шаг 4: Перезапустить бота
```bash
./restart.sh
```

---

## 📋 Проверка после деплоя:

### 1. Проверить логи запуска
```bash
tail -f logs/bot.log | grep -E "(АВТОМИГРАЦИЯ|Запуск|USE_OPENAI_RESPONSES)"
```

**Ожидаемый вывод (при USE_OPENAI_RESPONSES=False):**
```
USE_OPENAI_RESPONSES=False
--- 🚀 Запуск Telegram бота ---
```

**Если включить флаг (USE_OPENAI_RESPONSES=True) и есть user_threads.json:**
```
⚠️ АВТОМИГРАЦИЯ: USE_OPENAI_RESPONSES=True, но найден только user_threads.json
⚠️ Создаю пустой user_conversations.json. Пользователи начнут с новых conversations.
⚠️ Найдено 6 пользователей в user_threads.json
✅ Файл user_conversations.json создан (пустой)
```

### 2. Тестовые команды
Откройте Telegram и отправьте боту:
- `/reset` — должен ответить "🔄 Диалог сброшен!"
- `/reset_all` (только для админа) — должен показать счетчики для threads И conversations

---

## 🔄 Как включить новый API (когда будете готовы):

### 1. Изменить .env
```bash
USE_OPENAI_RESPONSES="True"
```

### 2. Перезапустить
```bash
./restart.sh
```

### 3. Проверить логи автомиграции
```bash
tail -100 logs/bot.log | grep "АВТОМИГРАЦИЯ"
```

### 4. Отправить тестовое сообщение
Бот создаст новый conversation через Responses API

---

## 🔙 Откат (если что-то пойдет не так):

```bash
# Просто выключить флаг
sed -i 's/USE_OPENAI_RESPONSES="True"/USE_OPENAI_RESPONSES="False"/' .env
./restart.sh
```

Бот вернется на старый API (Threads/Runs), все данные user_threads.json сохранены.

---

## ⚠️ Известные ограничения:

1. **При переключении на новый API пользователи начнут с чистых conversations** (контекст теряется)
2. **Исторические сообщения из файлов будут подгружаться** при создании нового conversation (функция `replay_history_to_conversation`)
3. **Нельзя "перенести" существующий thread в conversation** — это разные API

---

## 📊 Текущее состояние файлов:

- `user_threads.json` — **существует** (6 пользователей)
- `user_conversations.json` — **не существует** (будет создан при первом запуске с новым API)
- Флаг `USE_OPENAI_RESPONSES` — **False** (старый API активен)

---

## ✅ Готовность к деплою: **100%**

Все критические баги исправлены. Можно безопасно деплоить на сервер.

