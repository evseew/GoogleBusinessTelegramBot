# 🔄 Скрипты синхронизации с 1С

## Быстрый старт

### Проверить статус синхронизации

```bash
# Проверить crontab
crontab -l

# Проверить последние обновления
ls -lh data/*.json

# Посмотреть последние логи
tail -50 logs/sync/hourly.log
```

### Ручной запуск

```bash
# Почасовая (transactions)
./scripts/sync_1c_hourly.sh

# Ежедневная (clients + contracts)
./scripts/sync_1c_daily.sh
```

## Расписание cron

- **Каждый час** (00:00) — синхронизация транзакций
- **Каждый день** (06:00) — синхронизация клиентов и договоров

## Логи

- `logs/sync/hourly.log` — почасовая синхронизация
- `logs/sync/daily.log` — ежедневная синхронизация

## ⚠️ Важно для macOS

Cron требует разрешения **Full Disk Access**:
1. Системные настройки → Конфиденциальность
2. Full Disk Access → добавить `/usr/sbin/cron`
3. Перезагрузить систему

Подробности: `docs/SYNC_1C_SETUP.md`




